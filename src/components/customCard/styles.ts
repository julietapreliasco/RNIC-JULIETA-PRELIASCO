import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: 'pink',
    margin: 12,
    borderRadius: 5,
    padding: 20,
  },
  title: {
    fontSize: 20,
    paddingBottom: 10,
  },
});
